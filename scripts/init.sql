-- CREATE TABLE IF NOT EXISTS tools (
--   "id" TEXT NOT NULL,
--   "name" TEXT NOT NULL,
--   "email" TEXT NOT NULL,

--   PRIMARY KEY ("id")
-- );

-- Seed
-- INSERT INTO toos (id, name, email) VALUES ('userid', 'Gopher', 'hello@gopher.com');